<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permiso de Internet: sin él la aplicación no puede salir a la red. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        NOTA: NO se usa android:usesCleartextTraffic="true", porque habilitaría
        el tráfico HTTP sin cifrar para toda la aplicación. En su lugar, el
        atributo android:networkSecurityConfig de <application> apunta a
        res/xml/network_security_config.xml, donde el tráfico en claro se
        permite ÚNICAMENTE para los hosts del entorno de desarrollo
        (10.0.2.2, 127.0.0.1 y localhost). Cualquier otro dominio exige HTTPS.
    -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AplicacionMovilBasica"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AplicacionMovilBasica">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>